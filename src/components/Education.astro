---
const history = [
  {
    name: "Licenciatura en Ciencia de Datos",
    place: "Universidad Nac. Guillermo Brown",
    start: "2021",
    finish: null,
    progress: 0.7667,
  },
];
---

<section class="items-center sm:items-start">
  <h1 class="title">Educación 📓</h1>
  <ul class="w-full sm:w-3/5 pt-7 sm:pt-5 sm:px-3 list-none list-inside">
    {
      history.map((item, index) => (
        <li class="flex flex-col border-b border-secondary pb-0.5 mb-5">
          <a
            href="https://www.unab.edu.ar/lic-ciencia-datos/"
            target="_blank"
            rel="noopener noreferrer"
          >
            <h2 class="text-xl sm:text-2xl font-semibold hover:underline">
              .{item.name}
            </h2>
          </a>
          <h3 class="flex flex-col sm:flex-row font-bold text-secondary text-lg pl-3">
            {item.start} - {item.finish ? item.finish : "Actualidad"}
            <>
              <span class="text-primary px-1 hidden sm:block">/</span>
              <hr class="opacity-0" />
            </>
            {item.place}
          </h3>
        </li>
      ))
    }
  </ul>
</section>
